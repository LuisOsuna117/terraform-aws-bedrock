# trivy.yaml — configuration for `trivy config` IaC scans
# https://aquasecurity.github.io/trivy/latest/docs/references/configuration/config-file/
#
# Note: TRIVY_SKIP_CHECK_UPDATE=true is set in the GitHub Actions workflow step
# env block to bypass the broken aws/ec2/specify_ami_owners.rego in the
# downloaded policy bundle. The embedded checks are used instead.
